// Copyright 2023 UNN-IASR
#include "Automata.h"

int main() {
    Automata automata;
    automata.On();
    automata.Coin(140);
    automata.Choice(2);
    automata.Check();
    automata.Cook();
    automata.Finish();
    automata.Off();
    return 0;
}
